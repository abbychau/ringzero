# Reference manifest for the winget package (Abbychau.RingZero).
#
# The live manifests live in microsoft/winget-pkgs under
# manifests/a/Abbychau/RingZero/<version>/. The "Publish to winget" workflow
# (winget-releaser / komac) updates them automatically on every tagged GitHub
# release once the package exists in winget-pkgs and a WINGET_TOKEN secret
# (GitHub PAT with public_repo scope) is configured.
PackageIdentifier: Abbychau.RingZero
PackageVersion: 0.4.0
InstallerType: portable
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/abbychau/ringzero/releases/download/v0.4.0/ringzero-win-x64.exe
    InstallerSha256: 6E1BFE53FA995551AA72D9E6FA96D008E473235594BACB1BA89E169A158D6BF7
    Commands:
      - ringzero
ManifestType: installer
ManifestVersion: 1.5.0

// Stub for ink's optional `react-devtools-core` peer dependency.
// Ink only imports this when `process.env.DEV === 'true'`; the standalone
// bun build aliases it here so bundling stays self-contained.
export default {
  initialize() {},
  connectToDevTools() {},
};
